version: '3'

services:
  api:
    image: api
    ports:
      - "5000-5010:80"
    build:
      context: .
      dockerfile: API/Dockerfile
  silo:
    image: silo
    build:
      context: .
      dockerfile: Silo/Dockerfile
