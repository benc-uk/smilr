parameters:
  waitTime: ''
  dependsOn: ''

jobs:
  - job: waitJob
    dependsOn: ${{ parameters.dependsOn }}
    displayName: 'Delaying for ${{ parameters.waitTime }} seconds'

    pool:
      vmImage: Ubuntu-16.04

    steps:
    - bash: sleep ${{ parameters.waitTime }}
